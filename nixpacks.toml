[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
  "cd backend && python -m venv --copies venv",
  "cd backend && venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "cd backend && venv/bin/python app.py"
