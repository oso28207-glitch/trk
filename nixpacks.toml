[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
  "cd backend && python -m venv --copies /opt/venv",
  "cd backend && source /opt/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = "cd backend && source /opt/venv/bin/activate && python app.py"
