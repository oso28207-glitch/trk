[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
  "cd backend && python -m ensurepip --upgrade",
  "cd backend && python -m pip install --break-system-packages -r requirements.txt"
]

[start]
cmd = "cd backend && python app.py"
